{
  "name": "sdk-v0",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run build:packages",
    "build:packages": "bun run --cwd packages/core build && bun run --cwd packages/utils build",
    "test": "bun test",
    "dev": "bun run --watch index.ts",
    "clean": "rm -rf packages/*/dist"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "dependencies": {
    "@sdk-v0/core": "workspace:*"
  }
}
